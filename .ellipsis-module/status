. $module_path/.ellipsis-module/list-addons

status $HOME/.vim/addons/vice
status $HOME/.vim/addons/vim-addon-manager

for addon in `cat $module_path/tmp/addons-list`; do
    if [ -e "$addon" ]; then
        status $addon
    fi
done
