[core]
    editor = ~/.vim/bin/git-editor

[merge]
    tool = vim

[mergetool]
    keepBackup = false

[mergetool "vim"]
    cmd = vim -u ~/.vimgitrc -f -c "Gvdiff" $MERGED

[color]
	diff = never

# vim: ft=gitconfig
