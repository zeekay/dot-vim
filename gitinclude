[core]
    editor = git-editor

[merge]
    tool = vim

[mergetool]
    keepBackup = false

[mergetool "vim"]
    cmd = vim -u ~/.vimgitrc "+Gdiff" $MERGED -c '"call vice#git#merge()"'

# vim: ft=gitconfig
